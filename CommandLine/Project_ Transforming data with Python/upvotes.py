# What headline length leads to the most upvotes?
# What submission time leads to the most upvotes?
# How are the total numbers of upvotes changing over time?


from read import load_data
#from times import get_hour

df = load_data()
# _______________________________
# make a headline length column
df['headline_len'] =

# make a dict with headline length as key,
# total upvotes for that headline length as value

# get the headline length with the most upvotes

# _______________________________
# make a dict with submission_hour as key,
# total upvotes for that headline length as value


# _______________________________
#